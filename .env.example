# --- App ---
NEXT_PUBLIC_APP_NAME=Parte Facil
NEXT_PUBLIC_ENV=development

# Si usas auth o claves de APIs, añade aquí (no subir valores reales)
# AUTH_SECRET=
# NEXT_PUBLIC_ANALYTICS_ID=

# PDF/Export (si alguna lib requiere config por env)
# PDF_WORKER_URL=

# --- NOTAS ---
# Copia este archivo a .env.local y rellena las variables reales para desarrollo.
# Nunca subas .env.local al repo.
